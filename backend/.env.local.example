# Configuration pour un MongoDB local lancé avec `docker compose up -d`.
# Aucun secret Atlas ici : l'instance locale n'a ni utilisateur ni mot de passe.
MONGODB_URI=mongodb://127.0.0.1:27017/guitar-practice-cloud
JWT_SECRET=replace-with-a-long-random-local-secret
PORT=3000
